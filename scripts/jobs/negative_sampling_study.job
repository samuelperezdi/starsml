# /bin/sh
# ----------------Parameters---------------------- #
#$ -S /bin/sh
#$ -pe mthread 4
#$ -q lThM.q
#$ -l mres=384G,h_data=96G,h_vmem=96G,himem
#$ -cwd
#$ -j y
#$ -N neg_study_hyperopt
#$ -o negative_sampling_study.log
#$ -m bea
#$ -M vperezdiaz@cfa.harvard.edu
#
# ----------------Modules------------------------- #
#
# ----------------Your Commands------------------- #
#
echo + `date` job $JOB_NAME started in $QUEUE with jobID=$JOB_ID on $HOSTNAME
echo + NSLOTS = $NSLOTS
#

module load tools/conda
start-conda
conda activate starsml
python3 /scratch/public/sao/vdiaz/workspace/starsml/scripts/negative_sampling_study.py --experiment_name neg_study_def

#
echo = `date` job $JOB_NAME done